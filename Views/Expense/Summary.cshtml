<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Expense Summary - Expense Splitter</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); min-height: 100vh; padding: 20px; }
        .container { background: white; border-radius: 10px; padding: 40px; box-shadow: 0 10px 40px rgba(0,0,0,0.2); max-width: 600px; margin: 0 auto; }
        h1 { color: #667eea; margin-bottom: 30px; text-align: center; }
        .summary-box { background: #f8f9fa; border: 2px solid #667eea; border-radius: 10px; padding: 20px; margin: 20px 0; text-align: center; }
        .summary-box h3 { color: #764ba2; margin: 10px 0; }
        .member-item { background: #e6f0ff; padding: 15px; margin: 10px 0; border-radius: 5px; text-align: center; }
    </style>
</head>
<body>
    <div class="container">
        <a href="/Expense/Index/@ViewBag.GroupId" class="btn btn-outline-secondary mb-3">← Back to Expenses</a>

        <h1>💸 Expense Summary</h1>

        <div class="summary-box">
            <h3>Total Expenses</h3>
            <p style="font-size: 32px; color: #667eea; margin: 10px 0;">₹@ViewBag.Total.ToString("F2")</p>
        </div>

        <div class="summary-box">
            <h3>Equal Split Per Person</h3>
            <p style="font-size: 32px; color: #48bb78; margin: 10px 0;">₹@ViewBag.Share.ToString("F2")</p>
        </div>

        @if (ViewBag.Members != null && ViewBag.Members.Count > 0)
        {
            <h4>People in this trip:</h4>
            @foreach (var member in ViewBag.Members)
            {
                <div class="member-item">
                    <strong>@member</strong>
                </div>
            }
        }

        <div class="mt-4">
            <a href="/Expense/Index/@ViewBag.GroupId" class="btn btn-primary w-100">Back to Expenses</a>
        </div>
    </div>
</body>
</html>
