<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Expense - Expense Splitter</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); min-height: 100vh; padding: 20px; }
        .form-container { background: white; border-radius: 10px; padding: 40px; box-shadow: 0 10px 40px rgba(0,0,0,0.2); max-width: 500px; margin: 0 auto; }
        h1 { color: #667eea; margin-bottom: 30px; text-align: center; }
        .btn-success { background: #48bb78; border: none; width: 100%; }
        .btn-success:hover { background: #38a169; }
    </style>
</head>
<body>
    <div class="form-container">
        <h1>Add Expense</h1>
        <form method="post">
            <div class="mb-3">
                <label class="form-label">Expense Title</label>
                <input type="text" name="Title" class="form-control" placeholder="e.g., Dinner, Fuel, Hotel" required />
            </div>
            <div class="mb-3">
                <label class="form-label">Amount</label>
                <input type="number" name="Amount" class="form-control" placeholder="0.00" step="0.01" required />
            </div>
            <div class="mb-3">
                <label class="form-label">Paid By (Name)</label>
                <input type="text" name="PaidBy" class="form-control" placeholder="Your name" required />
            </div>
            <input type="hidden" name="GroupId" value="@ViewBag.GroupId" />
            <button type="submit" class="btn btn-success btn-lg mt-3">Add Expense</button>
            <a href="/Expense/Index/@ViewBag.GroupId" class="btn btn-outline-secondary btn-lg mt-2 w-100">Cancel</a>
        </form>
    </div>
</body>
</html>
